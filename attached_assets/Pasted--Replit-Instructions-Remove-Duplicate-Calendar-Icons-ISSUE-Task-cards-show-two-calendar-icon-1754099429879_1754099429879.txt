# Replit Instructions: Remove Duplicate Calendar Icons

**ISSUE: Task cards show two calendar icons - need to keep only the silhouette/outline version**

## Step 1: Find the Calendar Icon Components

Search for calendar icon references in the task card component:

```bash
# Search for calendar-related icons
grep -r "calendar" --include="*.tsx" --include="*.jsx"
grep -r "📅" --include="*.tsx" --include="*.jsx"  # Emoji calendar
grep -r "Due in" --include="*.tsx" --include="*.jsx"  # Near due date text
```

## Step 2: Identify the Two Icons

Look for code that renders both icons. It likely looks like:

```tsx
// WRONG - Shows both icons
<div className="due-date">
  <CalendarIcon className="colored" />  // Colored/filled icon
  📅  // Emoji calendar
  Due in {daysRemaining} days
</div>

// OR
<div>
  <span className="calendar-icon">📅</span>  // Colored emoji
  <Icon name="calendar-outline" />  // Silhouette
  <span>Due in {days} days ({date})</span>
</div>
```

## Step 3: Remove the Colored Calendar

Keep only the outline/silhouette version:

```tsx
// CORRECT - Only silhouette icon
<div className="due-date">
  <CalendarOutlineIcon />  // Or whatever the outline icon is called
  <span>Due in {daysRemaining} days ({formattedDate})</span>
</div>

// OR if using icon fonts/libraries
<div className="due-date">
  <i className="icon-calendar-outline"></i>
  <span>Due in {daysRemaining} days ({formattedDate})</span>
</div>
```

## Step 4: Check Different Task States

Make sure to fix it in all states (the red arrow shows a "Pending" task):

```tsx
// For pending tasks
{task.status === 'pending' && (
  <div className="due-date">
    <CalendarOutlineIcon />  // Only outline
    <span>Due in {daysRemaining} days</span>
  </div>
)}

// For in-progress tasks
{task.status === 'in-progress' && (
  <div className="due-date">
    <CalendarOutlineIcon />  // Only outline
    <span>Due in {daysRemaining} days</span>
  </div>
)}
```

## Step 5: Remove from CSS

If the colored icon is added via CSS, remove it:

```css
/* REMOVE this if it exists */
.due-date::before {
  content: "📅";  /* Remove emoji calendar */
}

/* KEEP this */
.calendar-outline {
  /* Silhouette icon styles */
}
```

## Step 6: Verify Consistency

Check that all task cards use the same icon style:
- ✅ Silhouette/outline calendar icon
- ❌ No colored/filled calendar icons
- ❌ No calendar emojis (📅)

The fix is simply removing one of the two calendar icons from the render code, keeping only the silhouette version for a cleaner, more consistent look.