5. CONSISTENT SUB-TAB STYLES:
css/* Sub-tab Navigation - Matching Production Data */
.sub-tab-navigation {
  display: flex;
  gap: 8px;
  margin-bottom: 24px;
  overflow-x: auto;
  overflow-y: hidden;
  padding-bottom: 4px;
  
  /* Hide scrollbar but keep functionality */
  -ms-overflow-style: none;
  scrollbar-width: none;
}

.sub-tab-navigation::-webkit-scrollbar {
  display: none;
}

.sub-tab-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 10px 20px;
  background: #f3f4f6;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 500;
  color: #6b7280;
  white-space: nowrap;
  transition: all 0.2s;
  flex-shrink: 0;
}

.sub-tab-btn:hover {
  background: #e5e7eb;
  color: #374151;
}

.sub-tab-btn.active {
  background: #2D8028;
  color: white;
}

/* Page Actions (Export buttons, etc) */
.page-actions {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 20px;
}

.btn-export {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 10px 20px;
  background: #2D8028;
  color: white;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 500;
  transition: all 0.2s;
}

.btn-export:hover {
  background: #236020;
}

/* Tab Content */
.tab-content {
  animation: fadeIn 0.3s ease-in-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Remove old inconsistent styles */
.analytics-nav,
.task-data-tabs,
.staff-data-tabs {
  display: none; /* Remove these old styles */
}

/* Mobile Responsive */
@media (max-width: 768px) {
  .sub-tab-navigation {
    margin: 0 -16px 20px -16px;
    padding: 0 16px 4px 16px;
  }
  
  .page-actions {
    margin: -8px -16px 16px -16px;
    padding: 0 16px;
  }
}
6. REMOVE OLD TAB STYLES:
Remove these inconsistent tab implementations:

Task Data's .analytics-nav with underline style
Staff Data's side-by-side layout
Any custom tab styling that doesn't match Production Data

SUMMARY:
All pages with sub-navigation now have:

✅ Consistent tab style - Pills with icons, green when active
✅ Same spacing and sizing - Matching Production Data exactly
✅ Export/Action buttons in top right when needed
✅ Smooth transitions between tabs
✅ Horizontal scrolling for many tabs (hidden scrollbar)
✅ Clean, modern look across all pages