📋 Simplify Pause and Skip - Just Status Changes
You're right - let's keep it simple. Just change the task status like we did with complete:
Make pause and skip work exactly like complete - they just change the task status:

1. UPDATE handleTaskAction FOR PAUSE:
   When pause is clicked, just change status to 'paused':
   ```javascript
   case 'pause':
     setTasks(prev => prev.map(t => 
       t.id === taskId 
         ? { 
             ...t, 
             status: 'paused',
             pausedAt: new Date().toISOString()
           }
         : t
     ));
     setShowTaskModal(false);
     break;

UPDATE handleTaskAction FOR SKIP:
Skip needs a reason first. In the modal, when Skip Task is clicked:

Show a text input for the reason
When reason is submitted, update the task:

javascriptconst handleSkipTask = (taskId: number, reason: string) => {
  setTasks(prev => prev.map(t => 
    t.id === taskId 
      ? { 
          ...t, 
          status: 'skipped',
          skippedAt: new Date().toISOString(),
          skipReason: reason
        }
      : t
  ));
  setShowTaskModal(false);
};

UPDATE TASK CARD BUTTONS:
Add these status checks to the task card:
jsx{task.status === 'paused' && (
  <button onClick={() => handleTaskAction(task.id, 'start')} className="btn-yellow">
    ▶️ Resume
  </button>
)}

{task.status === 'skipped' && (
  <button onClick={() => handleTaskAction(task.id, 'view')} className="btn-gray">
    View Details
  </button>
)}

UPDATE TASK CARD COLORS:
Add background colors based on status:
css/* Paused - yellow background */
.task-card[data-status="paused"],
.task-card.paused {
  background-color: #fef3c7; /* Light yellow */
}

/* Skipped - gray background */
.task-card[data-status="skipped"],
.task-card.skipped {
  background-color: #f3f4f6; /* Light gray */
}

UPDATE VIEW DETAILS MODAL:
When viewing a skipped task, show the skip reason:
jsx{task.status === 'skipped' && (
  <div className="skip-info">
    <p>⏭️ Skipped at: {formatDate(task.skippedAt)}</p>
    <p><strong>Reason:</strong> {task.skipReason}</p>
  </div>
)}

{task.status === 'paused' && (
  <div className="pause-info">
    <p>⏸️ Paused at: {formatDate(task.pausedAt)}</p>
  </div>
)}

FOR RESUME:
When a paused task is resumed (clicking Resume button):
javascriptcase 'resume':
  setTasks(prev => prev.map(t => 
    t.id === taskId 
      ? { 
          ...t, 
          status: 'in-progress',
          resumedAt: new Date().toISOString()
          // Keep pausedAt for time calculations later
        }
      : t
  ));
  // Reopen the modal
  setSelectedTask({ ...task, status: 'in-progress' });
  setShowTaskModal(true);
  break;


That's it! Just status changes:

Pause → status: 'paused' (yellow card)
Skip → status: 'skipped' (gray card, shows View Details)
Resume → status: 'in-progress' (back to normal)

All tasks end up with View Details button except in-progress (Collaborate) and pending (Start Task).