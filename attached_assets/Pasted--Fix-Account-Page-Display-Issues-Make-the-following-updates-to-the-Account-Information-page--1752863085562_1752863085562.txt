# Fix Account Page Display Issues

Make the following updates to the Account Information page:

## 1. Change "EMPLOYEE ID" to "STAFF ID"

Find where it says "EMPLOYEE ID" and change it to "STAFF ID":

```jsx
// Change from:
<p className="label">EMPLOYEE ID</p>

// To:
<p className="label">STAFF ID</p>
```

## 2. Fix Assigned Location Logic

Update the location display to show the correct location based on user role:

```jsx
// In the Account component, update the location logic:

const getAssignedLocation = (user) => {
  // Corporate users see "Grow Space Vertical Farms"
  if (user.role === 'Corporate') {
    return 'Grow Space Vertical Farms';
  }
  
  // All other users see their actual location
  return user.location || 'Kenosha';
};

// Then in the JSX:
<div className="info-card">
  <h3>💼 Work Information</h3>
  <p className="label">ASSIGNED LOCATION</p>
  <p className="value">{getAssignedLocation(currentUser)}</p>
  {/* ... rest of the work info ... */}
</div>
```

## 3. Update User Data and Staff ID Format

Update the Staff ID format to use location code + number (e.g., K001 for Kenosha):

```javascript
const users = [
  {
    id: 1,
    name: 'Alex Martinez',
    role: 'Staff',
    email: 'alex@growspace.farm',
    employeeId: 'K001', // Kenosha staff member 001
    location: 'Kenosha',
    // ... other fields
  },
  {
    id: 2,
    name: 'Dan Wilson',
    role: 'Manager',
    email: 'dan@growspace.farm',
    employeeId: 'K002', // Kenosha staff member 002
    location: 'Kenosha',
    // ... other fields
  },
  {
    id: 3,
    name: 'Matt Carlson',
    role: 'Corporate',
    email: 'matt@growspace.farm',
    employeeId: 'C001', // Corporate staff member 001
    location: 'Corporate', // or could be 'Kenosha' if based there
    // ... other fields
  }
];
```

Location code format:
- K = Kenosha
- R = Racine  
- LG = Lake Geneva
- C = Corporate

## 4. Change "Activity Summary" to "Task Summary"

Update the section title:

```jsx
// Change from:
<h2>📊 Activity Summary</h2>

// To:
<h2>📊 Task Summary</h2>
```

## 5. Add More Top Padding

Add more space between the header and the content:

```css
/* In the Account page styles, update or add: */
.account-container {
  padding: 40px 24px; /* Increase top padding from current value */
  /* or */
  padding-top: 40px; /* If you want to only change top padding */
}

/* Alternative: Add margin to the first element */
.account-header {
  margin-top: 24px; /* Add space before the content starts */
}
```

## Summary of Changes:
1. "EMPLOYEE ID" → "STAFF ID"
2. Location shows "Kenosha" for staff/managers, "Grow Space Vertical Farms" for Corporate
3. Alex Martinez is assigned to Kenosha
4. "Activity Summary" → "Task Summary"  
5. Added more top padding to the page

Test by:
- Viewing as Alex (Staff) - should show "Kenosha"
- Viewing as Matt Carlson (Corporate) - should show "Grow Space Vertical Farms"
- Checking that all text changes are applied
- Verifying the spacing looks better