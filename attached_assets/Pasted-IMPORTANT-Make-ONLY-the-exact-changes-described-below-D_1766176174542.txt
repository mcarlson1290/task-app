IMPORTANT: Make ONLY the exact changes described below. Do not modify any other code. Do not delete any data.
Task: Remove devOverdueProtection feature and auto-skip overdue tasks feature

CHANGE 1 of 3: Remove localStorage check from dateUtils.ts
File: client/src/utils/dateUtils.ts
Find this code (around lines 124-127):
tsx  // Check dev overdue protection mode
  if (typeof window !== 'undefined' && localStorage.getItem('devOverdueProtection') === 'true') {
    return false;
  }
DELETE those 4 lines entirely. Do not replace with anything.

CHANGE 2 of 3: Remove auto-skip useEffect from Tasks.tsx
File: client/src/pages/Tasks.tsx
Find the useEffect that starts with this comment (should be after the autoSkipOverdueMutation definition):
tsx// Automatic overdue task skipping - with guard to prevent loops
// NOTE: This must be defined AFTER autoSkipOverdueMutation
React.useEffect(() => {
  // Don't run if mutation is already in progress
  if (autoSkipOverdueMutation.isPending) return;
  if (!tasks || tasks.length === 0) return;

  const overdueTaskIds = tasks
    .filter(
      (task) =>
        task.status !== "completed" &&
        task.status !== "approved" &&
        task.status !== "skipped" &&
        isTaskOverdue(task),
    )
    .map((task) => task.id);

  if (overdueTaskIds.length > 0) {
    autoSkipOverdueMutation.mutate(overdueTaskIds);
  }
}, [tasks, autoSkipOverdueMutation.isPending]);
DELETE this entire useEffect block.

CHANGE 3 of 3: Remove autoSkipOverdueMutation from Tasks.tsx
File: client/src/pages/Tasks.tsx
Find and DELETE this entire mutation (around lines 352-372):
tsx  // Auto-skip overdue tasks mutation
  const autoSkipOverdueMutation = useMutation({
    mutationFn: async (taskIds: number[]) => {
      const promises = taskIds.map((taskId) =>
        apiRequest("PATCH", `/api/tasks/${taskId}`, {
          status: "skipped",
          skipReason: "Automatically skipped - overdue",
        }),
      );
      return await Promise.all(promises);
    },
    onSuccess: (_, taskIds) => {
      // Just invalidate - don't call refreshTasks() to prevent loops
      queryClient.invalidateQueries({ queryKey: ["/api/tasks"] });
      console.log(`Automatically skipped ${taskIds.length} overdue tasks`);
    },
    onError: (error) => {
      console.error("Error skipping overdue tasks:", error);
    },
  });
DELETE this entire mutation block.

DO NOT make any other changes. Confirm when complete.