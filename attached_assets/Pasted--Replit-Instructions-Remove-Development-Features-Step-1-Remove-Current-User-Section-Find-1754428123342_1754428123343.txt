# Replit Instructions: Remove Development Features

## Step 1: Remove Current User Section

Find the sidebar/navigation component and remove the Current User section:

```tsx
// REMOVE this entire section from the sidebar:
<div className="current-user-section">
  <div className="section-label">Current User:</div>
  <div className="user-info">
    <span>Alex Martinez (Staff)</span>
  </div>
  <div className="user-info">
    <span>Dan Wilson (Manager)</span>
  </div>
  <div className="user-info">
    <span>Matt Carlson (Corporate)</span>
  </div>
</div>

// Also remove the logout button if it's part of the dev features
<button className="logout-btn">Logout</button>
```

## Step 2: Remove Location Indicator

Remove the location indicator from the bottom of the sidebar:

```tsx
// REMOVE this from the sidebar:
<div className="location-indicator">
  <span className="location-icon">📍</span>
  <span>Location:</span>
  <div className="location-name">Kenosha</div>
</div>

// Remove associated CSS:
.location-indicator {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 16px;
  background: rgba(0, 0, 0, 0.1);
  // ... etc
}
```

## Step 3: Remove Test Locations

Update your location data to only include real locations:

```tsx
// In your mockData.ts or locationService.ts
export const locations = [
  { id: 'kenosha', name: 'Kenosha', isActive: true },
  // REMOVE: { id: 'racine', name: 'Racine', isActive: true },
];

// Or if you have a default location constant:
export const DEFAULT_LOCATION = 'Kenosha'; // Not 'Racine'
```

## Step 4: Clean Up Location References

Search and replace any hardcoded references to Racine:

```bash
# Search for Racine references in your code
grep -r "Racine" src/
grep -r "racine" src/
```

Replace any found instances with "Kenosha" or remove them if they're test data.

## Step 5: Simplify Navigation

Your sidebar should now look cleaner:

```tsx
const Sidebar = () => {
  return (
    <div className="sidebar">
      <div className="logo-section">
        <h1>🌱 Grow Space</h1>
      </div>
      
      <nav className="nav-menu">
        <Link to="/tasks">📋 Tasks</Link>
        <Link to="/account">👤 Account</Link>
        <Link to="/inventory">📦 Inventory</Link>
        <Link to="/education">🎓 Education</Link>
        <Link to="/recurring-tasks">🔄 Recurring Tasks</Link>
        
        <div className="nav-divider"></div>
        
        <Link to="/task-data" className="coming-soon">
          📊 Task Data <span className="soon-badge">Soon</span>
        </Link>
        <Link to="/staff-data">👥 Staff Data</Link>
        <Link to="/production-data" className="coming-soon">
          🌱 Production Data <span className="soon-badge">Soon</span>
        </Link>
      </nav>
      
      {/* No current user section */}
      {/* No location indicator */}
    </div>
  );
};
```

## Step 6: Update Header if Needed

If you're showing location in the header, update it:

```tsx
// Instead of showing location selector, just show the current location
<div className="header">
  <span>📍 Kenosha</span> {/* Simple, no dropdown */}
  <span>Welcome, {currentUser.name}</span>
</div>
```

## Step 7: Remove User Switching

If you have any user switching functionality for development:

```tsx
// REMOVE any of these:
- User dropdown selectors
- "Switch User" buttons  
- Mock login screens
- Test user lists
```

## Clean CSS

Remove any CSS related to these features:

```css
/* REMOVE these classes if they exist: */
.current-user-section { }
.user-switcher { }
.location-indicator { }
.location-selector { }
.dev-tools { }
.mock-login { }
```

This will give you a clean, production-ready interface without the development clutter!