# 🎯 Export Button Alignment Fix

## Align export buttons with sub-navigation tabs

### 1. CREATE NAVIGATION WITH ACTIONS WRAPPER:

```css
/* Navigation with Actions Container */
.nav-with-actions {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-xl);
  gap: var(--space-lg);
}

/* Sub-navigation stays on left */
.sub-tab-navigation {
  display: flex;
  gap: var(--space-sm);
  margin-bottom: 0; /* Remove bottom margin since parent handles it */
  padding: 6px;
  background: #f3f4f6;
  border-radius: 10px;
  flex: 1;
  overflow-x: auto;
  
  -ms-overflow-style: none;
  scrollbar-width: none;
}

.sub-tab-navigation::-webkit-scrollbar {
  display: none;
}

/* Actions stay on right */
.nav-actions {
  display: flex;
  gap: var(--space-md);
  flex-shrink: 0;
}

/* Export button styling */
.btn-export {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 24px;
  background: #2D8028;
  color: white;
  border: none;
  border-radius: 8px;
  font-size: 15px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
  white-space: nowrap;
}

.btn-export:hover {
  background: #236020;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(45, 128, 40, 0.2);
}
```

### 2. UPDATE STAFF DATA COMPONENT:

```typescript
export const StaffData: React.FC = () => {
  const [activeTab, setActiveTab] = useState('edit');
  
  const tabs = [
    { id: 'edit', label: 'Staff Edit', icon: '✏️' },
    { id: 'analytics', label: 'Staff Analytics', icon: '📊' }
  ];
  
  return (
    <div className="staff-data-page">
      {/* Navigation and Actions on same line */}
      <div className="nav-with-actions">
        <SubTabNavigation 
          tabs={tabs}
          activeTab={activeTab}
          onTabChange={setActiveTab}
        />
        
        <div className="nav-actions">
          <button className="btn-export">
            📊 Export Staff Report
          </button>
        </div>
      </div>
      
      {/* Page content */}
      <div className="tab-content">
        {activeTab === 'edit' && <StaffEdit />}
        {activeTab === 'analytics' && <StaffAnalytics />}
      </div>
    </div>
  );
};
```

### 3. UPDATE PRODUCTION DATA COMPONENT:

```typescript
export const ProductionData: React.FC = () => {
  const [activeTab, setActiveTab] = useState('dashboard');
  
  const tabs = [
    { id: 'dashboard', label: 'Live Dashboard', icon: '📊' },
    { id: 'trays', label: 'Tray Tracking', icon: '📦' },
    { id: 'equipment', label: 'Equipment', icon: '🏭' },
    { id: 'crops', label: 'Crop Config', icon: '🌾' },
    { id: 'system', label: 'System Config', icon: '⚙️' }
  ];
  
  return (
    <div className="production-data-page">
      {/* Navigation and Actions on same line */}
      <div className="nav-with-actions">
        <SubTabNavigation 
          tabs={tabs}
          activeTab={activeTab}
          onTabChange={setActiveTab}
        />
        
        <div className="nav-actions">
          <button className="btn-export">
            📊 Export Report
          </button>
        </div>
      </div>
      
      {/* Page content */}
      <div className="tab-content">
        {/* Tab content here */}
      </div>
    </div>
  );
};
```

### 4. MOBILE RESPONSIVE DESIGN:

```css
/* Mobile: Stack navigation and actions */
@media (max-width: 768px) {
  .nav-with-actions {
    flex-direction: column;
    align-items: stretch;
    gap: var(--space-md);
  }
  
  .sub-tab-navigation {
    width: 100%;
  }
  
  .nav-actions {
    justify-content: flex-end;
  }
  
  .btn-export {
    flex: 1;
    justify-content: center;
  }
}

/* Tablet: Keep inline but reduce button text */
@media (max-width: 1024px) {
  .btn-export {
    padding: 10px 16px;
    font-size: 14px;
  }
  
  /* Hide text, show only icon on small screens */
  @media (max-width: 640px) {
    .btn-export span {
      display: none;
    }
    
    .btn-export {
      padding: 10px;
      min-width: auto;
    }
  }
}
```

### 5. ALTERNATIVE: INTEGRATED INTO TAB BAR (Optional):

If you prefer the export button integrated into the tab bar itself:

```css
/* Alternative: Export button inside tab navigation */
.sub-tab-navigation-integrated {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 6px;
  background: #f3f4f6;
  border-radius: 10px;
  margin-bottom: var(--space-xl);
}

.tab-group {
  display: flex;
  gap: var(--space-sm);
}

.integrated-action {
  margin-left: auto;
  padding: 8px 16px;
  background: #2D8028;
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
}
```

### SUMMARY:

The export button is now:
- ✅ **Aligned horizontally** with the sub-navigation tabs
- ✅ **Stays on the right side** with proper spacing
- ✅ **Visually balanced** - not floating awkwardly
- ✅ **Mobile responsive** - stacks vertically on small screens
- ✅ **Consistent styling** across Staff Data and Production Data pages

This creates a much cleaner header area where navigation and actions are clearly organized on the same visual level.