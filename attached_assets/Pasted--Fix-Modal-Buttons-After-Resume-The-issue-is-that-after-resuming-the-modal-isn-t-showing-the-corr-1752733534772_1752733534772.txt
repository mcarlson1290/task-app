📋 Fix Modal Buttons After Resume
The issue is that after resuming, the modal isn't showing the correct buttons. Here's the fix:
Fix the modal to always show the correct buttons based on task status:

1. UPDATE THE MODAL BUTTON LOGIC:
   The modal should check the current task status, not how it was opened:
   
   ```jsx
   const TaskModal = ({ task, onClose }) => {
     if (!task) return null;
     
     // Key fix: Always check current task status
     const showActionButtons = task.status === 'in-progress';
     const allChecklistComplete = task.checklistItems === task.checklistCompleted;
     
     return (
       <div className="modal">
         <h2>{task.title}</h2>
         
         {/* Checklist section */}
         {task.checklistItems > 0 && (
           <div className="checklist">
             <h3>Checklist ({task.checklistCompleted || 0}/{task.checklistItems})</h3>
             {/* Checklist items */}
           </div>
         )}
         
         {/* Action buttons - only show for in-progress tasks */}
         {showActionButtons && (
           <div className="modal-actions">
             {/* Complete button - only if checklist is done */}
             {allChecklistComplete && (
               <button onClick={() => handleTaskAction(task.id, 'complete')} className="btn-complete">
                 ✅ Complete Task
               </button>
             )}
             
             {/* Pause button - always available */}
             <button onClick={() => handleTaskAction(task.id, 'pause')} className="btn-pause">
               ⏸️ Pause Task
             </button>
             
             {/* Skip button - always available */}
             <button onClick={() => setShowSkipReason(true)} className="btn-skip">
               ⏭️ Skip Task
             </button>
           </div>
         )}
         
         {/* Skip reason input (shows when skip is clicked) */}
         {showSkipReason && (
           <div className="skip-reason-section">
             <textarea
               placeholder="Reason for skipping (required)"
               value={skipReason}
               onChange={(e) => setSkipReason(e.target.value)}
             />
             <button 
               onClick={() => handleSkipWithReason(task.id, skipReason)}
               disabled={!skipReason.trim()}
             >
               Confirm Skip
             </button>
             <button onClick={() => setShowSkipReason(false)}>
               Cancel
             </button>
           </div>
         )}
         
         <button onClick={onClose}>Close</button>
       </div>
     );
   };

UPDATE RESUME TO REFRESH MODAL DATA:
When resuming, make sure to pass the updated task:
javascriptcase 'resume':
  const updatedTask = {
    ...task,
    status: 'in-progress',
    resumedAt: new Date().toISOString()
  };
  
  setTasks(prev => prev.map(t => 
    t.id === taskId ? updatedTask : t
  ));
  
  // Pass the UPDATED task to modal
  setSelectedTask(updatedTask);
  setShowTaskModal(true);
  break;

TRACK CHECKLIST COMPLETION:
Add checklist tracking to the task:
javascriptconst handleChecklistItem = (taskId: number, itemIndex: number) => {
  setTasks(prev => prev.map(t => {
    if (t.id === taskId) {
      const completed = t.checklistCompleted || 0;
      return {
        ...t,
        checklistCompleted: completed + 1
      };
    }
    return t;
  }));
};


The key changes:

Complete button only shows when ALL checklist items are done
Pause and Skip buttons ALWAYS show for in-progress tasks
Modal checks the current task status, not a stale reference

