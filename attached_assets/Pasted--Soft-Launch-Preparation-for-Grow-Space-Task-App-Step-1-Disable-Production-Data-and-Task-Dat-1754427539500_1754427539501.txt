# Soft Launch Preparation for Grow Space Task App

## Step 1: Disable Production Data and Task Data Tabs

Update your navigation menu to show "Coming Soon" on hover:

```tsx
// In your Navigation component
const menuItems = [
  { path: '/tasks', label: 'Tasks', icon: '📋', enabled: true },
  { path: '/account', label: 'Account', icon: '👤', enabled: true },
  { path: '/inventory', label: 'Inventory', icon: '📦', enabled: true },
  { path: '/education', label: 'Education', icon: '🎓', enabled: true },
  { path: '/recurring-tasks', label: 'Recurring Tasks', icon: '🔄', enabled: true, managerOnly: true },
  { path: '/task-data', label: 'Task Data', icon: '📊', enabled: false, comingSoon: true, managerOnly: true },
  { path: '/staff-data', label: 'Staff Data', icon: '👥', enabled: true, managerOnly: true },
  { path: '/production-data', label: 'Production Data', icon: '🌱', enabled: false, comingSoon: true, managerOnly: true },
];

// In the render
{menuItems.map(item => (
  <div key={item.path} className={`menu-item ${!item.enabled ? 'disabled' : ''}`}>
    {item.enabled ? (
      <Link to={item.path}>
        <span>{item.icon}</span> {item.label}
      </Link>
    ) : (
      <div className="coming-soon-item" title="Coming Soon">
        <span>{item.icon}</span> {item.label}
        <span className="coming-soon-badge">Soon</span>
      </div>
    )}
  </div>
))}
```

Add CSS for the disabled state:

```css
.menu-item.disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.coming-soon-item {
  display: flex;
  align-items: center;
  padding: 12px 16px;
  color: #999;
  position: relative;
}

.coming-soon-item:hover::after {
  content: "Coming Soon";
  position: absolute;
  left: 100%;
  margin-left: 10px;
  background: #333;
  color: white;
  padding: 4px 8px;
  border-radius: 4px;
  white-space: nowrap;
  z-index: 1000;
}

.coming-soon-badge {
  margin-left: auto;
  font-size: 10px;
  background: #f0f0f0;
  padding: 2px 6px;
  border-radius: 3px;
}
```

## Step 2: Add Beta/Testing Banner

Add a banner to indicate this is in testing:

```tsx
// Add to App.tsx or Layout component
<div className="beta-banner">
  🚧 Beta Version - We're testing and improving! Please report any issues to Matt.
</div>
```

```css
.beta-banner {
  background: #fff3cd;
  color: #856404;
  padding: 10px;
  text-align: center;
  border-bottom: 1px solid #ffeeba;
  position: sticky;
  top: 0;
  z-index: 100;
}
```

## Step 3: Deploy on Replit

For the soft launch, we'll use Replit's default domain:

1. **Make sure your app runs properly** in the Replit environment
2. **Share the Replit link** with your test users:
   - Your URL will be: `https://[your-repl-name].[your-username].repl.co`
   - Example: `https://growspace-taskapp.mattcarlson.repl.co`
3. **Keep the Repl running** - Pin it or use "Always On" if available

*Note: We'll set up the custom domain (growspace.farm/taskapp) in a future phase after testing.*

## Step 4: Pre-Launch Checklist

Before going live:

- [ ] Test all user roles (Regular, Manager, Corporate)
- [ ] Clear any test data that shouldn't be visible
- [ ] Set up error logging (add Sentry or similar)
- [ ] Create initial user accounts for testers
- [ ] Document known issues for testers
- [ ] Set up feedback collection method

## Step 5: Quick Fixes for Launch

Add error boundaries to prevent crashes:

```tsx
class ErrorBoundary extends React.Component {
  state = { hasError: false };
  
  static getDerivedStateFromError(error) {
    return { hasError: true };
  }
  
  render() {
    if (this.state.hasError) {
      return (
        <div className="error-page">
          <h2>Something went wrong</h2>
          <p>Please refresh the page or contact support.</p>
        </div>
      );
    }
    return this.props.children;
  }
}

// Wrap your app
<ErrorBoundary>
  <App />
</ErrorBoundary>
```

## Step 6: User Communication

Create a simple welcome message for first-time users:

```tsx
// Check if first visit
useEffect(() => {
  const hasVisited = localStorage.getItem('hasVisitedBeta');
  if (!hasVisited) {
    alert('Welcome to Grow Space Task App Beta! This is a test version - please report any issues to Matt.');
    localStorage.setItem('hasVisitedBeta', 'true');
  }
}, []);
```

## Recommended Domain Setup

For the smoothest setup:
1. Use Replit's deployment with custom domain (Option A)
2. Replit handles SSL, scaling, and updates automatically
3. You just need to add one CNAME record to your DNS

Would you like me to provide more specific instructions based on where your growspace.farm domain is currently hosted?